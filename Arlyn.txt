Iniciamos con la solución de la función principal "to_init_lol" y nos dimos cuenta que sería más fácil concatenar los datos del archivo de texto si los manipulamos desde strings y después lo convertimos a entero con la funcion de atoi. Después nos pasamos a la parte de crear la lista que funciona a traves de nodos que guarda cada elemento del archivo de texto. Esta parte fue la que más se me complico ya que cada elemento lo tenía que guardar como string y me confundía para guardarlo en un diferente nodo. 